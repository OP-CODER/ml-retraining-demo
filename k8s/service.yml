apiVersion: v1
kind: Service
metadata:
  name: ml-model-service
spec:
  selector:
    app: ml-model
  ports:
    - name: web-ui
      port: 8501
      targetPort: 8501
    - name: api
      port: 8000
      targetPort: 8000
  type: {{SERVICE_TYPE}}
