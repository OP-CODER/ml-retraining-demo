apiVersion: v1
kind: Service
metadata:
  name: ml-model-service
spec:
  selector:
    app: ml-model
  ports:
    - name: http
      port: 80
      targetPort: 8501   # Change to 8000 if you want API instead of Streamlit
  type: LoadBalancer
